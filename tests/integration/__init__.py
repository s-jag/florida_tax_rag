"""Integration tests package.

These tests require Docker services (Neo4j, Weaviate, Redis) to be running.
Use `make docker-up && make docker-wait` before running integration tests.

Run with: pytest -m integration
"""
